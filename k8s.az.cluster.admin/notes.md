# [azure k8s cluster ops](https://docs.microsoft.com/en-us/azure/aks/scale-cluster)

```
- Cluster operations

            - Scale the node count in AKS
            - [Auto scale in AKS](https://docs.microsoft.com/en-us/azure/aks/cluster-autoscaler)
            - Scale an AKS cluster
            - Upgrade an AKS cluster
            - Use Uptime SLA
            - Use proximity placement groups (preview)
            - Upgrade the node image (preview)
            - Process node OS updates (Apply security and kernel updates)
            - Configure an AKS cluster
            - Integrate ACR with an AKS cluster
            - Create virtual nodes
                    - Use the Azure CLI
                    - Use the Azure portal
            - Use Cluster Autoscaler
            - Use Availability Zones
            - Use node pools
            - Deploy AKS with Terraform
            - Use the Kubernetes dashboard

- Configure data volumes
            - Azure Disk 
                    - Dynamic
                    - Static
            - Azure Files 
                    - Dynamic
                    - Static
            - NFS Server - Static
            - Azure NetApp Files

- Configure Networking
      - Create or use exisiting virtual network 
                        - Use kubenet
                        - Use Azure-CNI
      - Create an internal load balancer

      - Use a Standard Load Balancer

      - Use a static IP address

      - Ingress
                - Create a basic controller
                - Use HTTP application routing
                - Use internal network
                - Enable the AGIC add-on for an existing AKS cluster
                - Use TLS with your own certificates

       - Use TLS with Lets Encrypt
                - Create an HTTPS ingress controller
                - Use a dynamic public IP address
                - Use a static public IP address

       - Egress
                - Restrict and control cluster egress traffic
                - Use a user defined route for egress
                - Use a basic load balancer and static IP address

       - Customize CoreDNS 


- Security Authentication
- Monitoring and Logging
- Use Windows Server Containers
- Develop and run applications
- select and deploy service mesh
- DevOps
- Troubleshoot

```
